<%= render 'shared/errors', obj: @style %>  

<div class = "row">
    <div class = "well col-md-8 col-md-offset-2">
      <%= form_for @style do |f| %>
        <%= f.label :name %>
        <%= f.text_field :name %>
        
        
        <div class="col-md-6">
          <%= f.submit(@style.new_record? ? 'Create Style' : 'Edit Style', class: "btn btn-success") %>
        </div>
        <div class="col-md-6">
          <%= link_to 'Cancel',styles_path, class: "btn btn-warning btn-cancel" %>
        </div>
        
      <% end %>
      
    </div>
  
</div>